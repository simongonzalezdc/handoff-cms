{
  "name": "@cms/licensing-guard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fail-closed license policy guard for the CMS workspace. Loads the shipped allowlist.json as the single authoritative policy source, walks functional-core dependencies (dependencies + devDependencies + optional/peer) and reports missing, unknown, denied, or uninspectable licenses.",
  "license": "Apache-2.0",
  "bin": {
    "cms-licensing-guard": "./dist/index.js"
  },
  "files": [
    "dist",
    "allowlist.json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. --config vitest.config.ts packages/licensing-guard/test"
  }
}
